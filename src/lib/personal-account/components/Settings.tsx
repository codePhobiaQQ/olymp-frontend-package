import { UserMetadataForm } from '@lib/personal-account/lib/metadata'

export const Settings = () => {
  return (
    <div className="flex w-full flex-row">
      <UserMetadataForm />
    </div>
  )
}
